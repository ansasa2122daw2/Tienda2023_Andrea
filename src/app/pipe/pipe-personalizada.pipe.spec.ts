import { PipePersonalizadaPipe } from './pipe-personalizada.pipe';

describe('PipePersonalizadaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePersonalizadaPipe();
    expect(pipe).toBeTruthy();
  });
});
